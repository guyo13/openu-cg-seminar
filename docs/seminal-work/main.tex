% Seminar summary — skeleton ONLY: structure, environments, figure wiring,
% and per-section prompts pointing at YOUR OWN existing text.
% Port the body into the professor's Overleaf template
% (https://www.overleaf.com/read/yrkcfvnvvxzd#3508dd) — if its preamble
% differs, keep HER preamble and paste the \section skeleton + figures.
% Guidelines mapped: Part 1 = Sec 1; Part 2 = Secs 2-4; Part 3 = Sec 5;
% Part 4 = references. Font 12pt, margins <= 2.5cm (set below in case the
% template doesn't).

\documentclass[12pt]{article}
\usepackage[margin=2.5cm]{geometry}
\usepackage{amsmath, amssymb, amsthm}
\usepackage{graphicx}
\usepackage[hidelinks]{hyperref}
\usepackage{float}
\graphicspath{{figs/}}  % adjust to where you place the repo's figs/ tree

\newtheorem{theorem}{Theorem}
\newtheorem{lemma}{Lemma}
\newtheorem{remark}{Remark}
\newcommand{\C}{\mathcal{C}}
\newcommand{\Dk}{\mathcal{D}_k}

\title{The Maximum Clique Problem in a Disk Graph Made Easy\\
\large A seminar summary of Keil and Mondal, SoCG 2025}
\author{Guy Or \\ The Open University of Israel}
\date{\today}

\begin{document}
\maketitle

% =====================================================================
\section{Introduction}\label{sec:intro}

\paragraph{The problem.}
A Disk Graph is a graph where each vertex corresponds to a disk in the plane, and there is an edge between two vertices if and only if the corresponding disks intersect.
The Maximum Clique problem in a disk graph asks for the largest subset of vertices such that every pair of vertices in the subset is connected by an edge, meaning that all corresponding disks intersect with each other.
The central question addressed by Keil and Mondal's paper is whether this maximum clique problem can be computed in polynomial time for disk graphs with multiple distinct radii.
The authors consider the case of $k$ distinct radii and provide an algorithm, with runtime $O(n^{2k}\,\mathrm{poly}(n))$, that is polynomial for every fixed $k$ --- placing the problem, parameterized by $k$, in the class XP.
For the general case of arbitrary radii, the complexity of the problem remains unknown.
The paper's contribution is significant because it settles a question posed by Cabello in 2015: whether the case of two distinct radii is polynomial-time solvable.


\paragraph{Motivation and history.}
In the context of geometric intersection graphs, the maximum clique problem has been a long-standing challenge, particularly for disk graphs with varying radii.
The problem is known to be polynomial-time solvable for several related classes such as rectangles and trapezoids,
yet NP-hard for others such as rays, ellipses, triangles, grounded strings, and balls.
In 1990, Clark, Colbourn and Johnson showed that the case of unit disk graphs (where all disks have an equal radius) can be solved in polynomial time,
yet the complexity for disks of arbitrary radii has remained unresolved for over three decades.
In 2015, Cabello explicitly posed even the case of two distinct radii as an open question.
A factor-2 approximation via stabbing all disks with four points is known, and improving it below 1.99 was open even for two radii.
Disk Graphs have practical applications in wireless networks, where nodes can be represented as disks with varying transmission ranges, and finding the maximum clique can help optimize network connectivity and resource allocation.

\paragraph{Why the obvious approaches fail.}
In the general case, finding a maximum clique in a graph is NP-hard, yet the geometric principle used by Clark, Colbourn and Johnson for unit disks --- which relies on the concept of ``lenses'' ---
allows for a polynomial-time solution: for each guessed farthest pair of disks, the candidate disks split into two cliques that together are guaranteed to contain the maximum clique.
The maximum clique can then be recovered by running a maximum matching algorithm --- itself polynomial --- on the complement of the candidate graph.
The naive approach of applying the unit disk algorithm to each radius type separately and then combining the results fails because the maximum clique can contain disks of different radii, and thus is not guaranteed to be contained within any single radius type.

\begin{figure}[H]
  \centering
  \includegraphics[width=.5\linewidth]{chapter3/notation/type_classes_fig.png}
  \caption{\small The maximum clique contains disks of both types, it is not contained in any single type class.
  (Figure by the author; see Appendix~\ref{sec:ai}.)}
  \label{fig:typeclasses}
\end{figure}

Attempting to generalize the lens-based approach to multiple radii also fails, as the existence of distinct ``clique regions'' within the lens is no longer guaranteed.
Keil and Mondal demonstrate this in section 6.1 of their paper by examining several natural lens-based attempts and showing that they fail to yield the two-clique split that the reduction requires.

\paragraph{Results and roadmap.}
The main result of the paper is given in \textbf{Theorem 3.2.}:
A maximum clique in a disk graph with $k$ radius types and $n$ vertices is computable in $O(n^{2k}(f(n)+n^2))$ time, where $f(n)$ is the maximum matching time in a bipartite graph with $n$ vertices.
Specifically, for $k=2$ (and any fixed $k$) the runtime is polynomial, settling Cabello's question.
The remainder of this summary is organized as follows.
\textbf{Section~\ref{sec:prelim}} introduces the notation and the graph-theoretic tools used later.
\textbf{Section~\ref{sec:guessing}} presents the guessing framework and why the seemingly trivial step of choosing the anchors is, in fact, the heart of the problem.
\textbf{Section~\ref{sec:algo}} describes the algorithm: the geometric lemma, the construction of the slab, and the reduction to bipartite matching, illustrated on a running example.
\textbf{Section~\ref{sec:conclusions}} discusses the complexity-theoretic status of the result, subsequent work, and open problems.

% =====================================================================
\section{Preliminaries}\label{sec:prelim}
\paragraph{Definitions and notation.}
\begin{itemize}
    \item A disk graph $G$ is an intersection graph of disks in $\mathbb{R}^2$, where each vertex of $G$ corresponds to a disk and two vertices are adjacent in $G$ if and only if their disks intersect.
    \item $D_{a,r}$ a disk with a center at point $a$ and radius $r$.
    \item Given a pair of disks $D_{a,r}$ and $D_{b,r'}$ , their common intersection region is called a ``lens'' and is denoted as $L(D_{a,r}, D_{b,r'})$.
    \item For a pair of points $a$ and $b$, we denote the line passing through them as $\ell_{ab}$.
    \item By $ab$ and $|ab|$ we denote the line segment with endpoints $a, b$ and the Euclidean distance between $a$ and $b$, respectively.
    \item By $\ell^h_p$ and $\ell^v_p$ we denote the horizontal and vertical lines through a point $p$, respectively.
    \item By an upper slab $U_{ab}$ of $ab$, we denote the region above $ab$, which is bounded by the lines $\ell^v_a$ and $\ell^v_b$ . Similarly, by a lower slab $\overline{U}_{ab}$ we denote the region below $ab$, which is bounded by $\ell^v_a$ and $\ell^v_b$.
    \item $\mathcal{D}_k$ is a disk graph where the number of different types of radii is at most $k$. A $type\text{-}i\ disk$ is a disk of radius $r_i$.
    \item Given a disk graph $\mathcal{D}_k$ with radii $r_1<r_2<\cdots<r_k$, $C_i$ is a clique in $\mathcal{D}_k$ that contains only type-$i$ disks, $\mathcal{C}$ is a maximum clique in $\mathcal{D}_k$, and $\mathcal{C}_i$ is a maximal clique in $\mathcal{C}$ where all disks are of type $i$.
\end{itemize}

\paragraph{Basic facts and graph-theoretic tools.}
\textbf{Intersection condition for disks.} Any two disks $D_{a,r}$ and $D_{b,r'}$, intersect if and only if the distance between their centers is at most the sum of their radii, formally: $|ab| \leq r + r'$.

% [Guidelines: formal definitions MAY be copied from the paper.]
% Disk graph, D_{a,r}, adjacency <=> |ab| <= r1+r2, D_k, radius types,
% C_i vs \C_i, slabs (+ your three-half-planes bullet), and the
% graph-theory toolkit stated for later use: complement, Gallai
% (alpha = n - tau), Konig (tau = nu, bipartite). State Lemma 2.1 here or
% at the start of Sec 4 — your call.

% =====================================================================
\section{The Guessing Framework}\label{sec:guessing}
% YOUR SOURCES: the sorting-fallacy exchange ("leftmost/rightmost is a
% sentence from the analysis, not the algorithm"), your corrected Q3
% answer (soundness/completeness), the witness-size discussion.
% Structure: define Psi via the anchors of the (unknown) \C_i -> the
% circularity -> the sorting fallacy with YOUR s5/s4 example (Fig) ->
% resolution: enumeration, the per-type menu C(n,0)+C(n,1)+C(n,2) ->
% soundness vs completeness as named properties -> witness size 2k as
% what makes the price affordable.

% \begin{figure}[t]
%   \centering
%   \includegraphics[width=.75\linewidth]{chapter3/algorithm/sort_trap.png}
%   \caption{The sorting fallacy on our running example: the sorted extremes
%   of the type-1 disks ($s_5$, $s_4$) are not in the maximum clique; the
%   true anchors ($s_1$, $s_2$) are defined only by the unknown solution.
%   (Figure by the author; see Appendix~\ref{sec:ai}.)}
%   \label{fig:sorttrap}
% \end{figure}

% =====================================================================
\section{The Algorithm}\label{sec:algo}

\subsection{The geometric core: Lemma 2.1}
% State + prove IN YOUR OWN WORDS (you reproved it closed-book; write
% that proof). Reference your case figures if you include stills:
% chapter2/lemma21/*.  The "slide away from the closest point" phrasing
% is yours — use it.

\subsection{The construction}
% Psi, slabs, filter, X_i/Y_i definitions (your slide bullets), X and Y.
% Running example walk-through = the guidelines' suggested "algorithm run
% example": figs guess.png, slabs.png, filter.png.

\subsection{Survivors form two cliques: Lemma 3.1}
% YOUR "lower point's type supplies slab and anchors" rule + the relay.
% Figure: chapter3/algorithm/lemma31_relay.png (real numbers!).
% Remark idea: the proof never uses Psi \subseteq \C — soundness.

\subsection{From two cliques to one matching}
% The five-hop chain WITH ATTRIBUTIONS (qa-armor S1; Gallai and Konig in
% the right places; never "larger side"). Figure: complement.png with the
% 3-1=2 arithmetic. 
% Remark (your catch): the paper leaves the Psi-union step implicit —
% Psi-disks are compatible with every candidate, so K is max in X\cup Y
% iff K\cup\Psi is max overall; also the Psi pairwise-intersection check.

\subsection{Running time}
% Your ledger: per-type menu -> O(n^{2k}); per guess O(k^2)+O(nk)+O(n^2)
% +f(n); the Sigma n_i = n accounting. State Theorem 3.2 formally
% (copyable). One sentence scoping out Sections 4-5 of the paper.

% =====================================================================
\section{Conclusions and Discussion}\label{sec:conclusions}
% [Part 3: what did we see, importance, state of the art, improved
%  since?, open problems, YOUR ideas.]
% YOUR SOURCES: XP-vs-FPT slide (both syntactic forms), witness-size
% framing, the unification table (reproduce it — the professor loved it;
% end the paper on "the framework was right, the split was wrong").
% "Improved since" facts to process into your own prose (verify quotes
% against the sources before submitting):
%  - SWAT 2026 (Gao, Gawrychowski, Giannopoulos, Mulzer, Singh, Staals,
%    Zehavi): randomized (1-eps)-approx in ~O(n/eps^2) for unit disks; a
%    parameterized approximation scheme ~O(f(t)(1/eps)^{O(t)} n) for t
%    radii — improves on n^{2t} by allowing approximation+randomization;
%    EXACT complexity still open. \cite{gao2026swat}
%  - The SoCG 2025 proceedings version extends the technique to translates
%    of k distinct L-visible convex shapes. \cite{keil2025clique}
%  - Espenant-Keil-Mondal unit-disk paper: journal version in DCG 2026.
%    \cite{espenant2026dcg}
% Open problems: general radii (both directions), FPT vs XP gap, faster
% fixed-k algorithms. Your own idea candidates: you already voiced one in
% our discussions — e.g., the witness-enumeration barrier framing as the
% obstacle an FPT algorithm must break.

\begin{figure}[H]
  \centering
  \includegraphics[width=.5\linewidth]{chapter6/lens_deception.png}
  \caption{\small Generalizing the lens-based approach to multiple radii fails: the centers of the big disks are confined to a lens of radius $r_s + r_b$, but two big disks on the \emph{same side} of the line $\ell_{pq}$ may still fail to intersect --- breaking the property that the unit disk algorithm relies on.
  (Figure by the author; see Appendix~\ref{sec:ai}.)}
  \label{fig:lensdeception}
\end{figure}



\bibliographystyle{alpha}
\bibliography{references}

% =====================================================================
\section*{Declaration on the Use of AI Tools}\label{sec:ai}
% MANDATORY per the guidelines. Draft below — REVIEW, adjust to what is
% true, and keep it in your voice:
%
% "In preparing this seminar (talk and summary) I used Anthropic's Claude
% as follows: (i) exploring SoCG proceedings and selecting the paper;
% (ii) discussing the paper's proofs and testing my understanding through
% questions and quizzes; (iii) generating and iterating on the Python/
% matplotlib code that renders the original figures in this document (the
% examples and their computed cliques are verified programmatically; code
% at github.com/guyo13/openu-cg-seminar); (iv) searching for related and
% subsequent work; (v) improving the English phrasing of text I wrote.
% All prose in this summary was written by me; AI assistance was not used
% to generate passages of the text."

\end{document}